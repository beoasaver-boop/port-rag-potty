# RAG portable
#
# Servicio RAG local con bge-m3 (Ollama) + ChromaDB + grafo ligero.
# Portable: copia esta carpeta `rag/` dentro de cualquier proyecto.
#
# Endpoints: ver server.py (FastAPI).
# Config:    rag/config.py (se genera <PROJECT_ROOT>/rag.config.json al arrancar).